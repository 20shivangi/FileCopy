ghaeconfig:
  # Account name to use -- DNS record will be created with accountName.githubenterprise.com
  account: microsoft
  # Location of the instance
  location: eastus2
  # Number of seats
  instanceSize: 5000
  # Ring where this instance is deployed
  ring: ring-0
